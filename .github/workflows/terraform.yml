name: 'Terraform CI/CD Pipeline'

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read
  pull-requests: write

jobs:
  terraform-plan:
    name: 'Terraform Plan'
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Azure Login
      uses: azure/login@v2
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3

    - name: Terraform Init
      run: terraform init
      env:
        ARM_CLIENT_ID: "de021fdd-06ac-413f-b247-0868fad8089c"
        ARM_CLIENT_SECRET: ${{ secrets.AZURE_CREDENTIALS ? fromJson(secrets.AZURE_CREDENTIALS).clientSecret : '' }}
        ARM_SUBSCRIPTION_ID: "61e822a8-75be-427e-9026-e3b5d46fdd22"
        ARM_TENANT_ID: "a8a6d952-dc1e-4b83-97e1-18ff70df7777"

    - name: Terraform Plan
      run: terraform plan -input=false
      env:
        ARM_CLIENT_ID: "de021fdd-06ac-413f-b247-0868fad8089c"
        ARM_CLIENT_SECRET: ${{ secrets.AZURE_CREDENTIALS ? fromJson(secrets.AZURE_CREDENTIALS).clientSecret : '' }}
        ARM_SUBSCRIPTION_ID: "61e822a8-75be-427e-9026-e3b5d46fdd22"
        ARM_TENANT_ID: "a8a6d952-dc1e-4b83-97e1-18ff70df7777"

  terraform-apply:
    name: 'Terraform Apply'
    needs: terraform-plan
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    environment: production
    
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Azure Login
      uses: azure/login@v2
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3

    - name: Terraform Init
      run: terraform init
      env:
        ARM_CLIENT_ID: "de021fdd-06ac-413f-b247-0868fad8089c"
        ARM_CLIENT_SECRET: ${{ secrets.AZURE_CREDENTIALS ? fromJson(secrets.AZURE_CREDENTIALS).clientSecret : '' }}
        ARM_SUBSCRIPTION_ID: "61e822a8-75be-427e-9026-e3b5d46fdd22"
        ARM_TENANT_ID: "a8a6d952-dc1e-4b83-97e1-18ff70df7777"

    - name: Terraform Apply
      run: terraform apply -auto-approve -input=false
      env:
        ARM_CLIENT_ID: "de021fdd-06ac-413f-b247-0868fad8089c"
        ARM_CLIENT_SECRET: ${{ secrets.AZURE_CREDENTIALS ? fromJson(secrets.AZURE_CREDENTIALS).clientSecret : '' }}
        ARM_SUBSCRIPTION_ID: "61e822a8-75be-427e-9026-e3b5d46fdd22"
        ARM_TENANT_ID: "a8a6d952-dc1e-4b83-97e1-18ff70df7777"